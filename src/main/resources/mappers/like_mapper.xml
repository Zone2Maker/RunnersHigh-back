<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runnershigh.runnershigh.mapper.LikeMapper">
    <insert id="addLike">
        INSERT INTO
            like_tb
        VALUES
            (0, #{feedId}, #{userId}, now(), now())
    </insert>
    <delete id="removeLike">
        DELETE FROM
            like_tb
        WHERE
            feed_id = #{feedId} AND user_id = #{userId}
    </delete>
</mapper>


