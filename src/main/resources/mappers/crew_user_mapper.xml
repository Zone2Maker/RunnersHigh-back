<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runnershigh.runnershigh.mapper.CrewUserMapper">
    <insert id="joinCrew">
        INSERT INTO crew_user_tb (
            crew_user_id,
            crew_id,
            user_id,
            last_read_message_id,
            create_dt,
            update_dt
        SELECT
            0,
            #{crewId},
            #{userId},
            <!-- 들어가려는 크루의 마지막 메시지 id -->
            IFNULL((SELECT MAX(message_id) FROM message_tb WHERE crew_id = #{crewId}), 0),
            NOW(),
            NOW()
    </insert>
    <select id="getLastReadMessageId">
        SELECT
            last_read_message_id
        FROM
            crew_user_tb
        WHERE
            user_id = #{userId}
            AND
            crew_id = #{crewId}
    </select>

    <select id="getCrewUserByCrewIdAndUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM crew_user_tb
            WHERE crew_id = #{crewId} AND user_id = #{userId}
            )
    </select>
</mapper>