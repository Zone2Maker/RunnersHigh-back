<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runnershigh.runnershigh.mapper.CrewMapper">
    <insert id="addCrew" useGeneratedKeys="true" keyProperty="crewId">
        INSERT INTO crew_tb
            (crew_id, user_id, crew_name, crew_detail, crew_img_url, crew_region, crew_status, max_members, current_members, create_dt, update_dt)
        VALUES
            (0, #{userId}, #{crewName}, #{crewDetail}, #{crewImgUrl}, #{crewRegion}, "ACTIVE", #{maxMembers}, 1, now(), now() );
    </insert>
    <select id="getCrewList">
        SELECT * FROM crew_tb
        <where>
            <if test="search != null">
                (
                crew_name LIKE CONCAT('%', #{search}, '%')
                OR crew_detail LIKE CONCAT('%', #{search}, '%')
                OR crew_region LIKE CONCAT('%', #{search}, '%')
                )
            </if>
            <if test="region != null">
                AND crew_region = #{region}
            </if>
        </where>
    </select>
    <select id="getCrewByCrewId">
        SELECT
            *
        FROM
            crew_tb
        WHERE
            crew_id = #{crewId};
    </select>

</mapper>