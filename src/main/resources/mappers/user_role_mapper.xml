<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runnershigh.runnershigh.mapper.UserRoleMapper">
    <insert id="addUserRole">
        INSERT INTO
            user_role_tb
        VALUES
            (0, #{userId}, #{crewId}, NOW(), NOW())
    </insert>

    <update id="updateUserRole">
        UPDATE
            user_role_tb
        SET
            role_id = #{roleId}
        WHERE
            user_id = #{userId}
    </update>
</mapper>